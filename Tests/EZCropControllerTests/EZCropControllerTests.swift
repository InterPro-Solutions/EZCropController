import XCTest
@testable import EZCropController

final class EZCropControllerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EZCropController().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
